# Local builds, downloaded build dependencies, and verification artifacts.
/build/

# Godot-generated caches and imported translations. Keep .uid and .import files.
/client/.godot/
/client/**/*.translation
