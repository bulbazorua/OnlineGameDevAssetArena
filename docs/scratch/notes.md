* GPU based neural-network or AI - we need to offload where-ever we can , the computation needed for our complex AI

* We need observability for CPU usage and GPU usage. Each refactor - should have an observable data/statistics when we move a CPU bound logic into GPU.

* Two types of AI - we have auto-battler character AI, and then we have a `player` like AI that controls the character AI. This is equivalent to real human `player` that controls the strategy of the Character that they selected